# Copy this file to `.env` and change values.
# Never commit real secrets.

# Server port (uvicorn --port still wins if you pass it)
PORT=8000

# JWT secret for signing login tokens
JWT_SECRET_KEY=change-me-to-a-long-random-string

# Optional: token expiry minutes (default: 10080 = 7 days)
# ACCESS_TOKEN_EXPIRE_MINUTES=10080

# Optional: database url (default uses sqlite file ./labflow.db)
# DATABASE_URL=sqlite:///./labflow.db

# Optional: full path to typst.exe if it is not discoverable via PATH
# TYPST_BIN=C:\\Path\\To\\typst.exe

# ZJU DeepSeek API Key (required for AI features)
# Get your API key from ZJU AI platform
# Models: deepseek-v3, deepseek-r1-671b
ZJU_DEEPSEEK_API_KEY=your-zju-deepseek-api-key-here
